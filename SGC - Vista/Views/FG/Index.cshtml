@model IEnumerable<SGC___Modelo.FormatoGestion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProcesoGestion.codigoProceso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.codigo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProcesoGestion.codigoProceso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.codigo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCodigoFG }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCodigoFG }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCodigoFG })
        </td>
    </tr>
}

</table>
