@model IEnumerable<SGC___Modelo.Gestion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EstadoGestion.estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FormatoPlantilla.titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.anulada)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EstadoGestion.estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FormatoPlantilla.titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.anulada)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idGestion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idGestion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idGestion })
        </td>
    </tr>
}

</table>
