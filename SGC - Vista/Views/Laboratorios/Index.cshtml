@model IEnumerable<SGC___Modelo.Laboratorio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreLab)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.activo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreLab)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.activo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idLaboratorio }) |
            @Html.ActionLink("Details", "Details", new { id=item.idLaboratorio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idLaboratorio })
        </td>
    </tr>
}

</table>
